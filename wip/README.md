# wip/ — live working notes

- `methods_log.md` — consolidated lab notebook (location M0–M11 + identity ID-0–ID-6 eras);
  the fix-campaign era continues in `docs/critical-analysis/fixes-log.md`.
- `data_details.md` — dataset facts.
- `model_comparison.md` — P1 model study (RTMPose-L vs X, detector findings).
- `archive/` — resolved/executed documents (issue catalogues, implementation plans,
  recommendations). Everything still actionable lives in `/remaining-work.md` at the repo
  root; do not resurrect archived items from here.
